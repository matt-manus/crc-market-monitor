# Copy this file to .env only for local runs. Never commit .env.
MASSIVE_API_KEY=
# Keep this configurable because provider endpoint and plan details can change.
MASSIVE_BASE_URL=https://api.massive.com
